export class Post {

    private _title: string
    private _text: string
    private _image:string

    constructor(_title: string, _text:string, _image:string){
        this._title = _title
        this._text = _text
        this._image = _image
    }
}
